# 01-basics - Go Fundamentals

This folder contains the foundational concepts of Go programming.

## Topics Covered

- Variables (`variables.go`)
- Data Types (`types.go`)
- Constants (`const.go`)
- Type Conversions (`conversions.go`)
- Arrays (`arrays.go`)
- Slices (`slices.go`)
- Booleans (`booleans.go`)
- Operators (`operators.go`)
- Input (`input.go`)

## Purpose

This section builds the core understanding of how Go handles data, memory, and basic structures before moving into control flow and functions.

## Notes

- All files use `package main` for simplicity.
- Each file is independent and can be run using:

```bash
go run filename.go